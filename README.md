# Static Site Generator

This project aims to create a simple to use static site generator, written in
kotlin, to create modern html + css with Tailwind.

See [jacquelineberrios.com](https://jacquelineberrios.com)

`./gradlew publish` to publish to maven local  
`./gradlew jar` to build a jar, located in `lib/build/libs/`
